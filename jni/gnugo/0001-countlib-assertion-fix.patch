--- a/engine/board.c
+++ b/engine/board.c
@@ -1515,7 +1515,7 @@ countlib(int str)
 int
 countlib(int str)
 {
-  ASSERT1(IS_STONE(board[str]), str);
+  if (!IS_STONE(board[str])) return 0;
   
   /* We already know the number of liberties. Just look it up. */
   return string[string_number[str]].liberties;
