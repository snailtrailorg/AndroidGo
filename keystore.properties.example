# Release signing credentials for Google Play upload.
# Copy this file to keystore.properties and fill in your actual values.
# keystore.properties is gitignored — do NOT commit it.

storeFile=/home/michael/AndroidStudioProjects/AndroidGo/app/release.keystore
storePassword=your-store-password
keyAlias=androidgo
keyPassword=your-key-password
